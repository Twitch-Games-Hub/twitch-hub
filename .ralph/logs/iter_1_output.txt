There are still 4 pending tasks (17, 18, 19, 20), so I should NOT output RALPH_DONE.

**Task 16 is complete.** Here's what was implemented:

1. **Created `RankBadge.svelte`** — Pill-shaped badge component with metallic gradient backgrounds for all 5 rank tiers (BRONZE/SILVER/GOLD/PLATINUM/DIAMOND), bold uppercase rank name, thin tier-colored border, and 3 size variants (sm/md/lg). Diamond rank has a shimmer animation via scoped CSS keyframes.

2. **Updated profile page** (`u/[twitchLogin]/+page.svelte`) — Added `RankBadge` import and `rankTier` to the `ProfileData` interface. Badge displays next to the player's display name when `rankTier` is present. Also passes `rankTier` to the `XpBar` component.

3. **Updated `XpBar.svelte`** — Added optional `rankTier` prop that displays the rank name as uppercase text between the level badge and XP count in the non-compact view.

All acceptance criteria met:
- RankBadge renders a pill badge with metallic gradient styling
- All 5 rank tiers have distinct color schemes
- Diamond rank has shimmer animation
- Size variants sm/md/lg produce correctly sized badges
- Profile page shows rank badge next to display name
- XpBar optionally shows rank tier name alongside level
- `pnpm build` completes without errors
