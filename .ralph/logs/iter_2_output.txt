There are still 3 pending tasks (18, 19, 20), so I should NOT output RALPH_DONE.

**Task 17 is complete.** Here's what was implemented:

**Files created:**
- `apps/web/src/routes/leaderboard/+page.svelte` — New SvelteKit route with paginated global XP leaderboard

**Files modified:**
- `apps/web/src/routes/+layout.svelte` — Added "Leaderboard" link to both desktop and mobile navigation

**Acceptance criteria verified:**
1. Route `/leaderboard` renders a global XP leaderboard page
2. Page fetches from `/api/gamification/leaderboard` endpoint with limit/offset
3. Table shows rank, player avatar+name, rank badge (RankBadge component), level, and total XP
4. Top 3 players have visually highlighted rows (gold/silver/bronze backgrounds)
5. Player rows link to `/u/{twitchLogin}` profile pages
6. Pagination with Previous/Next works correctly (disabled at boundaries, "Page X of Y")
7. Loading state shows animated skeleton rows while data fetches
8. Navigation includes a Leaderboard link (both desktop and mobile)
9. `pnpm build` completes without errors
