Now let me verify that not all tasks are completed (tasks 12-20 are still pending), so I should NOT output RALPH_DONE.

Task 11 is complete. Here's what was implemented:

**Files changed:**
- `packages/shared-types/src/gamification.ts` — Added all new types, constants, and utility functions
- `packages/shared-types/src/socket-events.ts` — Added `gamification:round-xp` socket event

**What was added:**
1. **RankTier enum** — BRONZE, SILVER, GOLD, PLATINUM, DIAMOND
2. **RANK_TIER_THRESHOLDS** — Maps each tier to XP threshold (0/500/1500/3500/7000)
3. **computeRankTier(totalXp)** — Returns highest rank whose threshold is met (descending if-chain matching existing `computeLoyaltyTier` pattern)
4. **xpToNextRank(totalXp)** — Returns `{ current, next, xpNeeded }` tuple
5. **STREAK_MULTIPLIERS** — Constants `{ THRESHOLD_2X: 3, THRESHOLD_3X: 5, MULTIPLIER_2X: 2, MULTIPLIER_3X: 3 }`
6. **getStreakMultiplier(consecutiveCorrect)** — Returns 1 (<3), 2 (3-4), 3 (>=5)
7. **RoundXpSummary, RankUpEventData, XpTickEventData** — New interfaces
8. **GamificationEvent** — Extended type union with `'rank_up' | 'xp_tick'` and corresponding data types
9. **ServerToClientEvents** — Added `'gamification:round-xp'`
10. **PlayerProfileSummary** — Added `rankTier: RankTier` field

`pnpm build` completes without errors. All acceptance criteria met.
