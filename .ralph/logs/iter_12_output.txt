Task 12 is complete. Let me verify the acceptance criteria:

1. **getStreakMultiplier is imported and used in recordCorrectAnswer** — Imported from `@twitch-hub/shared-types`, called after `redis.incr(sKey)`.
2. **At streak 3-4, STREAK_BONUS = XP_AWARDS.CORRECT_ANSWER * 1 (5 XP)** — `multiplier=2`, `bonusXp = 5 * (2-1) = 5`.
3. **At streak >=5, STREAK_BONUS = XP_AWARDS.CORRECT_ANSWER * 2 (10 XP)** — `multiplier=3`, `bonusXp = 5 * (3-1) = 10`.
4. **Streak milestone broadcast includes multiplier** — Added `multiplier` to the event data object.
5. **StreakEventData includes optional multiplier field** — Added `multiplier?: number`.
6. **finalizeSession correctly sums variable STREAK_BONUS** — Uses `hgetall` to read accumulated totals; key name unchanged.
7. **pnpm build completes without errors** — Verified.

There are still pending tasks (13-20), so I do NOT output RALPH_DONE.
